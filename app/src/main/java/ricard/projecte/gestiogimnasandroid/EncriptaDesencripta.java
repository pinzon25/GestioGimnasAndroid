package ricard.projecte.gestiogimnasandroid;

import java.io.UnsupportedEncodingException;

public class EncriptaDesencripta {

    /*public static String Encriptar(String texto) throws UnsupportedEncodingException {

        return Base64.encodeBase64String(texto.getBytes("utf-8")); //devuelve un string codificado.
    }

    public static String Desencriptar(String textoEncriptado) throws Exception {
        byte[] decode = Base64.decodeBase64(textoEncriptado.getBytes()); //decodifica el string codificado y lo devuelve.
        return new String(decode, "utf-8");
    }*/
}
